services:
  - type: web
    name: ai-anemia-detection-clinical-report-generation
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
    runtime: python-3.10.13
